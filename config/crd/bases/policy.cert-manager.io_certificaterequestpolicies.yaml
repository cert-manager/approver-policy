apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.0
  creationTimestamp: null
  name: certificaterequestpolicies.policy.cert-manager.io
spec:
  group: policy.cert-manager.io
  names:
    categories:
      - cert-manager
    kind: CertificateRequestPolicy
    listKind: CertificateRequestPolicyList
    plural: certificaterequestpolicies
    shortNames:
      - crp
    singular: certificaterequestpolicy
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: CertificateRequestPolicy is the Schema for the certificaterequestpolicies API
          type: object
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              type: object
              properties:
                allowedCommonName:
                  type: string
                allowedDNSNames:
                  type: array
                  items:
                    type: string
                allowedEmailAddresses:
                  type: array
                  items:
                    type: string
                allowedIPAddresses:
                  type: array
                  items:
                    type: string
                allowedIsCA:
                  type: boolean
                allowedIssuer:
                  type: array
                  items:
                    description: ObjectReference is a reference to an object with a given name, kind and group.
                    type: object
                    required:
                      - name
                    properties:
                      group:
                        description: Group of the resource being referred to.
                        type: string
                      kind:
                        description: Kind of the resource being referred to.
                        type: string
                      name:
                        description: Name of the resource being referred to.
                        type: string
                allowedPrivateKey:
                  type: object
                  properties:
                    allowedAlgorithm:
                      type: string
                      enum:
                        - RSA
                        - ECDSA
                        - Ed25519
                    maxSize:
                      type: integer
                    minSize:
                      description: Values are inclusive (i.e. a min value with 2048 will accept a size of 2048). MinSize and MaxSize may be the same.
                      type: integer
                allowedSubject:
                  type: object
                  properties:
                    allowedCountries:
                      type: array
                      items:
                        type: string
                    allowedLocalities:
                      type: array
                      items:
                        type: string
                    allowedOrganizationalUnits:
                      type: array
                      items:
                        type: string
                    allowedOrganizations:
                      type: array
                      items:
                        type: string
                    allowedPostalCodes:
                      type: array
                      items:
                        type: string
                    allowedProvinces:
                      type: array
                      items:
                        type: string
                    allowedSerialNumber:
                      type: string
                    allowedStreetAddresses:
                      type: array
                      items:
                        type: string
                allowedURIs:
                  type: array
                  items:
                    type: string
                allowedUsages:
                  type: array
                  items:
                    description: 'KeyUsage specifies valid usage contexts for keys. See: https://tools.ietf.org/html/rfc5280#section-4.2.1.3      https://tools.ietf.org/html/rfc5280#section-4.2.1.12 Valid KeyUsage values are as follows: "signing", "digital signature", "content commitment", "key encipherment", "key agreement", "data encipherment", "cert sign", "crl sign", "encipher only", "decipher only", "any", "server auth", "client auth", "code signing", "email protection", "s/mime", "ipsec end system", "ipsec tunnel", "ipsec user", "timestamping", "ocsp signing", "microsoft sgc", "netscape sgc"'
                    type: string
                    enum:
                      - signing
                      - digital signature
                      - content commitment
                      - key encipherment
                      - key agreement
                      - data encipherment
                      - cert sign
                      - crl sign
                      - encipher only
                      - decipher only
                      - any
                      - server auth
                      - client auth
                      - code signing
                      - email protection
                      - s/mime
                      - ipsec end system
                      - ipsec tunnel
                      - ipsec user
                      - timestamping
                      - ocsp signing
                      - microsoft sgc
                      - netscape sgc
                maxDuration:
                  type: string
                minDuration:
                  description: Values are inclusive (i.e. a min value with 50s will accept a duration with 50s). MinDuration and MaxDuration may be the same.
                  type: string
            status:
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                      observedGeneration:
                        type: integer
                        format: int64
                      reason:
                        type: string
                      status:
                        description: ConditionStatus represents a condition's status.
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        type: string
      served: true
      storage: true
      subresources:
        status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
